// css와 연결 할때는 <link> 사용 
<link rel="stylesheet" href="css파일 위치">

// js와 연결 할때는 <script> 사용
<script src="js파일 위치"></script>


// documnet는 html코드 전부를 Object즉 객체로 받아옴
// ex) index.html - <title>blank</title>
//     app.js - console.log(documnet.title) => blank

// documnet.getElementById('..') 를 사용 할 경우 
// 태그의 id가 적힌 곳의 값을 출력

// documnet.querySelector은 css문법이 사용 가능
// documnet.querySelectorAll은 이름이 중복이 있어도 모두 가져 오고 배열로 바꿈

